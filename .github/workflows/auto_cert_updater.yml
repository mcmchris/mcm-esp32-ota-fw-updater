name: Actualizador Automático de Certificados

on:
  schedule:
    - cron: '0 8 * * 1' # Ejecutar todos los lunes a las 8 AM (o diario si prefieres)
  workflow_dispatch:    # Botón manual para forzar la actualización

jobs:
  update-certs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar dependencias
        run: |
          sudo apt-get update
          pip install click pyopenssl certifi

      - name: Descargar herramientas
        run: |
          wget https://raw.githubusercontent.com/OPEnSLab-OSU/SSLClient/master/tools/pycert_bearssl/pycert_bearssl.py
          wget https://raw.githubusercontent.com/OPEnSLab-OSU/SSLClient/master/tools/pycert_bearssl/cert_util.py

      - name: Generar nuevos certificados (MCM_CertStore.h)
        run: |
          echo "Descargando colección maestra de certificados..."

          # 1. AAA Certificate Services (Legacy para CDN de GitHub)
          wget -O aaa_services.crt "https://crt.sh/?d=331986"

          # 2. Starfield Class 2 (Legacy para AWS S3)
          wget -O starfield_class2.crt "https://crt.sh/?d=1063596"

          # 3. USERTrust ECC Certification Authority (GitHub API Actual)
          wget -O usertrust_ecc.crt "https://crt.sh/?d=2841410"

          # 4. DigiCert Global Root CA (Estándar de la industria / GitHub Backup)
          wget -O digicert_global.crt "https://crt.sh/?d=853"

          echo "Convirtiendo certificados a header C++..."
          
          # CAMBIO CLAVE: Usamos 'convert' en lugar de 'download'.
          # Esto procesa los archivos locales sin intentar conectarse a internet.
          
          python3 pycert_bearssl.py convert \
            *.crt \
            --output MCM_CertStore.h
            
          # LIMPIEZA
          rm *.crt *.py
          rm -rf __pycache__

      - name: Verificar cambios
        id: verify_diff
        run: |
          git diff MCM_CertStore.h

      - name: Crear Pull Request con los nuevos certificados
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "security: Actualización de Certificados (GitHub + Legacy CDN)"
          title: "🔐 Actualización de Certificados SSL (MCM_CertStore.h)"
          body: |
            Actualización de Trust Anchors generada automáticamente.
            
            **Contenido:**
            1. `api.github.com` (Escaneado en vivo).
            2. `AAA Certificate Services` (Inyectado para compatibilidad CDN).
            3. `Starfield Class 2` (Inyectado para compatibilidad AWS S3 Legacy).
          branch: update-ssl-certs
          base: main
          delete-branch: true
          add-paths: |
            MCM_CertStore.h